# requirements.txt
# 精益 SOP 助手依赖（离线版）
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 文档解析
pypdf>=6.0.0
python-pptx>=1.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
pywin32>=303      # 旧版 .doc 文件解析（需安装 Word/WPS）

# TF-IDF 向量化 + 检索
scikit-learn>=1.3.0

# 数值计算
numpy>=1.24.0

# UI
streamlit>=1.60.0

# API 调用
openai>=1.0.0
